syntax = "proto3";

package worldcup2026;

option go_package = "quiniela/proto/worldcuppb";

// Equipos aceptados en las predicciones.
enum Teams {
  TEAMS_UNKNOWN = 0;
  GTM = 1;
  MEX = 2;
  BRA = 3;
  ARG = 4;
  ESP = 5;
}

// Predicción enviada desde el cliente al servidor gRPC.
message MatchPredictionRequest {
  Teams home_team = 1;
  Teams away_team = 2;
  int32 home_goals = 3;
  int32 away_goals = 4;
  string username = 5;
  string timestamp = 6;
}

// Respuesta enviada por el servidor gRPC.
message MatchPredictionResponse {
  string status = 1;
}

// Servicio gRPC disponible.
service MatchPredictionService {
  rpc SendPrediction(MatchPredictionRequest)
      returns (MatchPredictionResponse);
}
