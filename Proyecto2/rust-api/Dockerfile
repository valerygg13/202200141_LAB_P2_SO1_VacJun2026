# Etapa 1: compilar la aplicación Rust
FROM rust:1.96-bookworm AS builder

WORKDIR /app

COPY rust-api/Cargo.toml rust-api/Cargo.lock ./
COPY rust-api/src ./src

RUN cargo build --release


# Etapa 2: ejecutar solamente el binario
FROM debian:bookworm-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        ca-certificates \
        libssl3 \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder \
    /app/target/release/rust-api \
    /usr/local/bin/rust-api

EXPOSE 8081

CMD ["rust-api"]